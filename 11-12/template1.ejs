<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <title>Анализ МИРЭА</title>
    <style>
        /* Основные стили */
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #1e1e2f;
            color: #f4f4f4;
            line-height: 1.6;
        }

        h1,
        h2 {
            text-align: center;
            font-weight: 700;
        }

        h1 {
            font-size: 2.5rem;
            color: #e63946;
            margin: 20px 0;
        }

        section {
            margin: 20px auto;
            max-width: 90%;
            background: #282a36;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }

        .section-title {
            font-size: 1.8rem;
            color: #50fa7b;
            margin-bottom: 20px;
            border-bottom: 2px solid #444;
            padding-bottom: 5px;
        }

        .count {
            font-size: 1.2rem;
            color: #8be9fd;
            margin-bottom: 20px;
        }

        /* Карточный стиль */
        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .card {
            background: #44475a;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            flex: 1 1 calc(33.333% - 20px);
            max-width: 300px;
            color: #f4f4f4;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
        }

        .card-title {
            font-size: 1.5rem;
            color: #ff79c6;
            margin-bottom: 10px;
        }

        .card-data {
            font-size: 1rem;
            color: #bd93f9;
        }

        /* Кнопки */
        button {
            display: block;
            background: #50fa7b;
            color: #282a36;
            padding: 10px 20px;
            margin: 20px auto;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        button:hover {
            background: #8be9fd;
        }

        /* Адаптивность */
        @media (max-width: 768px) {
            body {
                font-size: 0.9rem;
            }

            h1 {
                font-size: 2rem;
            }

            .card {
                flex: 1 1 calc(100% - 20px);
                max-width: 100%;
            }
        }
    </style>
</head>

<body>
    <h1>Анализ МИРЭА</h1>

    <section>
        <h2 class="section-title">Года</h2>
        <div class="count">Общее количество: <strong>
                <%= yearsCount %>
            </strong></div>
        <div class="card-container">
            <% uniqueYears.forEach(item=> { %>
                <div class="card">
                    <div class="card-title">Год: <%= item.year %>
                    </div>
                    <div class="card-data">Количество: <%= item.count %>
                    </div>
                </div>
                <% }); %>
        </div>
    </section>

    <section>
        <h2 class="section-title">Ссылки</h2>
        <div class="count">Общее количество: <strong>
                <%= sourceLinksCount %>
            </strong></div>
        <div class="card-container">
            <% uniqueSourceLinks.forEach(item=> { %>
                <div class="card">
                    <div class="card-title">Ссылка</div>
                    <div class="card-data"><a href="<%= item.link %>" style="color: #50fa7b; text-decoration: none;"
                            target="_blank">
                            <%= item.link %>
                        </a></div>
                    <div class="card-data">Количество: <%= item.count %>
                    </div>
                </div>
                <% }); %>
        </div>
    </section>

    <section>
        <h2 class="section-title">Денежные Суммы</h2>
        <div class="count">Общее количество: <strong>
                <%= moneySumsCount %>
            </strong></div>
        <div class="card-container">
            <% uniqueMoneySums.forEach(item=> { %>
                <div class="card">
                    <div class="card-title">Сумма: <%= item.sum %>
                    </div>
                    <div class="card-data">Количество: <%= item.count %>
                    </div>
                </div>
                <% }); %>
        </div>
    </section>
</body>

</html>